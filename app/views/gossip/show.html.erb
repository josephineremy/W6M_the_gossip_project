  <div class= "text-center"><br><h1>Voici le numéro du potin n°<%= @gossip.id %></h1><br></div>


    <!-- Card -->
    <div class="card card-cascade wider p-5">

      <!-- Card image -->
      <div class="view view-cascade gradient-card-header blue-gradient">

        <!-- Title -->
        <h2 class="card-header-title mb-3 p-2"><%= @gossip.title %></h2>
        <!-- Text -->
        <p class="mb-0 p-2"><i class="fas fa-user-check mr-2"></i><%= @gossip.user.first_name %> <%= @gossip.user.last_name %></p>
      </div>

      <!-- Card content -->
      <div class="card-body card-body-cascade text-center">

        <!-- Text -->
        <p><strong>Le potin: </strong><%= @gossip.content %></p>
        <!-- Link -->
        <%= link_to "Plus d'informations sur l'auteur", user_path(@gossip.user.id) %>
      </div>
      </div>
